name: Update Downloads Page

on:
  release:
    types: [published]
  push:
    branches: [ main ]
    paths-ignore:
      - 'docs/**'
      - '*.md'

jobs:
  update-downloads:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Get latest release info
      id: release
      run: |
        # Obtener información del último release
        LATEST_RELEASE=$(curl -s "https://api.github.com/repos/${{ github.repository }}/releases/latest")

        # Verificar si la respuesta contiene un mensaje de error
        if echo "$LATEST_RELEASE" | jq -e '.message' > /dev/null 2>&1; then
          HAS_RELEASE=false
        else
          HAS_RELEASE=true
        fi

        if [ "$HAS_RELEASE" = "true" ]; then
          # Si hay releases disponibles
          TAG_NAME=$(echo "$LATEST_RELEASE" | jq -r '.tag_name // "v0.0.0"')
          RELEASE_DATE=$(echo "$LATEST_RELEASE" | jq -r '.published_at' | cut -d'T' -f1)

          # Limpiar el body del release de caracteres problemáticos
          RELEASE_BODY=$(echo "$LATEST_RELEASE" | jq -r '.body // "Sin descripción"' | tr -d '\n\r' | sed 's/[*#]//' | cut -c1-100)

          # Buscar archivos .deb en los assets
          DEB_URL=$(echo "$LATEST_RELEASE" | jq -r '.assets[]? | select(.name | endswith(".deb")) | .browser_download_url' | head -1)
          DEB_NAME=$(echo "$LATEST_RELEASE" | jq -r '.assets[]? | select(.name | endswith(".deb")) | .name' | head -1)

          # Si no encuentra .deb, usar valores por defecto
          if [ -z "$DEB_URL" ] || [ "$DEB_URL" = "null" ]; then
            DEB_URL="https://github.com/${{ github.repository }}/releases/download/$TAG_NAME/package.deb"
            DEB_NAME="package.deb"
          fi

          {
            echo "tag_name=$TAG_NAME"
            echo "release_date=$RELEASE_DATE"
            echo "release_body=$RELEASE_BODY"
            echo "deb_url=$DEB_URL"
            echo "deb_name=$DEB_NAME"
            echo "has_release=true"
          } >> $GITHUB_OUTPUT
        else
          # Si no hay releases, usar información del commit más reciente
          COMMIT_DATE=$(git log -1 --format="%Y-%m-%d")
          COMMIT_MESSAGE=$(git log -1 --format="%s" | tr -d '\n\r' | cut -c1-50)
          COMMIT_SHA=$(git log -1 --format="%h")

          {
            echo "tag_name=v0.0.0-dev"
            echo "release_date=$COMMIT_DATE"
            echo "release_body=$COMMIT_MESSAGE (commit $COMMIT_SHA)"
            echo "deb_url=https://github.com/${{ github.repository }}/releases/download/latest/package.deb"
            echo "deb_name=package.deb"
            echo "has_release=false"
          } >> $GITHUB_OUTPUT
        fi

    - name: Update downloads page sections
      run: |
        echo "=== Debug: Valores obtenidos ==="
        echo "Tag: ${{ steps.release.outputs.tag_name }}"
        echo "Date: ${{ steps.release.outputs.release_date }}"
        echo "DEB URL: ${{ steps.release.outputs.deb_url }}"
        echo "DEB Name: ${{ steps.release.outputs.deb_name }}"
        echo "Has Release: ${{ steps.release.outputs.has_release }}"

        # Verificar que el archivo existe
        if [ ! -f "docs/downloads.md" ]; then
          echo "❌ El archivo docs/downloads.md no existe"
          exit 1
        fi

        echo "=== Archivo encontrado, aplicando cambios ==="

        # 1. Actualizar la URL del wget usando un método más seguro
        # Crear un archivo temporal con el nuevo contenido
        NEW_WGET_LINE="<!--URL_WGET-->wget ${{ steps.release.outputs.deb_url }}<!--END_URL_WGET-->"

        # Usar perl en lugar de sed para manejar mejor los caracteres especiales
        perl -pi -e 's|<!--URL_WGET-->.*?<!--END_URL_WGET-->|'"$NEW_WGET_LINE"'|g' docs/downloads.md

        # 2. Actualizar el nombre del archivo DEB
        NEW_DEB_LINE="<!--NOMBRE_DEB-->./${{ steps.release.outputs.deb_name }}<!--END_NOMBRE_DEB-->"
        perl -pi -e 's|<!--NOMBRE_DEB-->.*?<!--END_NOMBRE_DEB-->|'"$NEW_DEB_LINE"'|g' docs/downloads.md

        # 3. Verificar si la versión ya existe en el historial para evitar duplicados
        if ! grep -q "${{ steps.release.outputs.tag_name }}" docs/downloads.md; then
          echo "=== Agregando nueva versión al historial ==="

          # Buscar la línea del separador de tabla
          if grep -q "^|.*---|.*---|.*---" docs/downloads.md; then
            # Crear la nueva línea de historial
            NEW_HISTORY_LINE="| ${{ steps.release.outputs.tag_name }} | ${{ steps.release.outputs.release_date }} | ${{ steps.release.outputs.release_body }} |"

            # Usar awk para insertar después del separador
            awk -v new_line="$NEW_HISTORY_LINE" '
              /^[|].*---[|].*---[|].*---/ {
                print $0
                print new_line
                next
              }
              {print}
            ' docs/downloads.md > docs/downloads.md.tmp && mv docs/downloads.md.tmp docs/downloads.md

            echo "✅ Nueva versión agregada al historial"
          else
            echo "❌ No se encontró la tabla de historial de cambios"
          fi
        else
          echo "=== Versión ${{ steps.release.outputs.tag_name }} ya existe en el historial, no se agrega duplicada ==="
        fi

        echo "=== Cambios aplicados ==="

    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"

        echo "=== Estado del repositorio ==="
        git status

        # Verificar si hay cambios en el archivo
        if git diff --quiet docs/downloads.md; then
          echo "❌ No hay cambios en docs/downloads.md para commitear"
        else
          echo "✅ Cambios detectados en docs/downloads.md, commiteando..."

          git add docs/downloads.md
          git commit -m "🤖 Actualizar página de descargas

          - Versión: ${{ steps.release.outputs.tag_name }}
          - Fecha: ${{ steps.release.outputs.release_date }}
          - URL actualizada: ${{ steps.release.outputs.deb_url }}
          - Archivo: ${{ steps.release.outputs.deb_name }}"

          echo "=== Pushing cambios ==="
          git push
          echo "✅ Cambios enviados correctamente"
        fi
