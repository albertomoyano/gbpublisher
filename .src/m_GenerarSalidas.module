' Gambas module file

' ====================================================
' VARIABLES PRIVADAS DEL MÓDULO
' ====================================================
Private sRutaProyecto As String
Private sArchivoProyecto As String
Private sBaseNombreProyecto As String
Private sNombreArchivo As String
Private sFileCanonico As String
Private sSQL As String
Private rConsulta As Result
Private sComando As String
Private sSaxon As String
Private sEstiloCita As String
Private sRutaMedia As String

' ' ====================================================
' ' INICIALIZAR VARIABLES DESDE EL PROYECTO ACTIVO
' ' SE LLAMA AL INICIO DE CADA FUNCIÓN PÚBLICA
' ' ====================================================
' Private Sub InicializarVariables()
'
'   sSaxon = "/opt/Saxon-HE/saxon-he.jar"
'   sRutaProyecto = File.Dir(FMain.txtProyecto.Text)
'   sArchivoProyecto = File.Name(FMain.txtProyecto.Text)
'   sBaseNombreProyecto = File.BaseName(FMain.txtProyecto.Text)
'   sRutaMedia = "media"
'
'   ' OBTENER NOMBRE DEL ARCHIVO DEL ARTÍCULO ACTIVO
'   ' id_articulo SE ACTUALIZA AL SELECCIONAR UN ARTÍCULO EN EL COMBOBOX
'   sSQL = "SELECT nombre_archivo FROM articulos WHERE id_articulo = &1"
'   rConsulta = m_ConexionBD.mConn.Exec(sSQL, FMain.id_articulo.Value)
'   sNombreArchivo = m_XML.TrimUTF8(rConsulta["nombre_archivo"])
'
'   ' CONSTRUIR RUTA DEL CANÓNICO
'   ' nombre_archivo = "a-02-revistaAlgo-v01-n01"
'   ' sFileCanonico  = /ruta/proyecto/jats/c-02-revistaAlgo-v01-n01.xml
'   sFileCanonico = sRutaProyecto &/ "jats" &/ "c-" & Mid(sNombreArchivo, 3) & ".xml"
'
'   ' ESTILO DE CITA SEGÚN CONFIGURACIÓN DE LA REVISTA
'   Dim hRev As Result = m_ConexionBD.mConn.Exec(
'     "SELECT estilo_cita FROM revistas_md WHERE id_proyecto = &1 LIMIT 1",
'     CInt(FMain.id_proyecto.Value))
'   ' DETERMINAR EL TIPO DE CITA LEYENDO EL ATRIBUTO class DEL ARCHIVO CSL
'   ' class="in-text"  → autor-anio
'   ' class="numeric"  → vancouver
'   ' FUNCIONA CON CUALQUIER CSL DEL REPOSITORIO ZOTERO SIN IMPORTAR EL NOMBRE
'   Dim sCsl As String = "apa.csl"
'   If hRev.Available Then
'     Dim sCslBD As String = m_XML.TrimUTF8(hRev["estilo_cita"])
'     If sCslBD <> "" Then sCsl = sCslBD
'   Endif
'
'   ' REEMPLAZAR el bloque de LeerTipoCSL en InicializarVariables temporalmente:
'
'   Dim sRutaArchivoCSL As String = User.Home &/ ".gbpublisher" &/ "csl" &/ sCsl
'   FMain.TerminalViewProyecto.Input("echo 'DEBUG sCsl: " & sCsl & "'" & Chr(10))
'   FMain.TerminalViewProyecto.Input("echo 'DEBUG ruta: " & sRutaArchivoCSL & "'" & Chr(10))
'   FMain.TerminalViewProyecto.Input("echo 'DEBUG existe: " & CStr(Exist(sRutaArchivoCSL)) & "'" & Chr(10))
'   If Exist(sRutaArchivoCSL) Then
'     sEstiloCita = LeerTipoCSL(sRutaArchivoCSL)
'     FMain.TerminalViewProyecto.Input("echo 'DEBUG resultado LeerTipoCSL: " & sEstiloCita & "'" & Chr(10))
'   Else
'     sEstiloCita = "autor-anio"
'     FMain.TerminalViewProyecto.Input("echo 'DEBUG archivo no encontrado → fallback autor-anio'" & Chr(10))
'   Endif
'
'   ' Dim sRutaArchivoCSL As String = User.Home &/ ".gbpublisher" &/ "csl" &/ sCsl
'   ' If Exist(sRutaArchivoCSL) Then
'   '   sEstiloCita = LeerTipoCSL(sRutaArchivoCSL)
'   ' Else
'   '   sEstiloCita = "autor-anio"
'   ' Endif
'
' End

' ====================================================
' GENERAR PDF DESDE LATEX es para archivo LaTeX puro
' ====================================================
Public Sub GenerarPDFdesdeLATEX()

  If FMain.txtProyecto.Text = "" Then
    m_Sonido.sonar("Warning")
    Message.Warning("No hay ningún proyecto abierto")
    Return
  Endif

  ' EXPORTAR DEPENDENCIAS
  m_Json.ExportarMetadatosJSON2PDF()
  m_Bibtex.ExportarBibTeX()

  ' CARGAR Y CONCATENAR PREÁMBULO + CONTENIDO
  Dim sPreambulo As String = File.Load(File.Dir(FMain.txtProyecto.Text) &/ "files/pdf-preambulo.tex")
  Dim sContenido As String = File.Load(File.Dir(FMain.txtProyecto.Text) &/ File.Name(FMain.txtProyecto.Text))
  Dim sMain As String = sPreambulo & gb.NewLine & gb.NewLine & sContenido

  ' GUARDAR ARCHIVO TEMPORAL
  Dim outputFile As String = File.Dir(FMain.txtProyecto.Text) &/ "temporal.tex"
  File.Save(outputFile, sMain)

  ' COMPILAR CON LATEXMK
  FMain.TerminalViewProyecto.Input("cd " & File.Dir(FMain.txtProyecto.Text) & "\n")
  FMain.TerminalViewProyecto.Input("clear" & "\n")
  FMain.TerminalViewProyecto.Input(
    "latexmk -l -f -g --interaction=nonstopmode -pdflatex=lualatex -pdflua temporal.tex" & "\n")
  Wait 1

  FMain.TerminalViewProyecto.Input(
    "mv temporal.pdf " & File.Dir(FMain.txtProyecto.Text) &/ "salidas/pdf/" &
    File.BaseName(FMain.txtProyecto.Text) & ".pdf" & "\n")

  m_FuncionesGenericas.BorrarArchivosAuxiliares

End

' Public Function LeerTipoCSL(sRutaCSL As String) As String
'
'   ' MAPEO DIRECTO: nombre de archivo CSL → identificador de estilo
'   ' gbpublisher SOPORTA EXACTAMENTE 4 ESTILOS CON NOMBRES FIJOS
'   Select Case Lower(File.Name(sRutaCSL))
'     Case "apa.csl"
'       Return "apa"
'     Case "ieee.csl"
'       Return "ieee"
'     Case "vancouver.csl"
'       Return "vancouver"
'     Case "iso690-author-date-es.csl"
'       Return "iso690"
'     Case Else
'       Message.Error("Estilo de cita no reconocido: " & File.Name(sRutaCSL) & Chr(10) &
'         "Los estilos soportados son: apa.csl, ieee.csl, vancouver.csl, iso690-author-date-es.csl")
'       Return ""
'   End Select
'
' Catch
'   Return "apa"
'
' End Function
